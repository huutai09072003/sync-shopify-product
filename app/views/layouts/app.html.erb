<!DOCTYPE html>
<html>
  <head>
    <title>Picture It - Live Preview</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta http-equiv="content-type" content="text/html; charset=utf-8" >
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag 'app', media: 'all' %>
    <!-- Re-enable stylesheet pack tag if you will import CSS to app.js file -->
    <!-- stylesheet_pack_tag 'app' - opening and closing tags needed -->

    <% if content_for?(:head) %>
      <%= yield :head %>
    <% end %>
    <%= render partial: 'shared/script_tags' %>
  </head>
  <body>
    <%= yield %>

    <% if content_for?(:tail) %>
      <%= yield :tail %>
    <% end %>

    <%= render 'shared/footer' %>
  </body>
</html>
